mkdir express-demo
cd express-demo
npm init
npm install express express-generator --save

to start create a file index.js inside this module

use $ npm start index.js
 or install and run nodemon to restart your app after every change
npm i -g nodemon

to start with nodemon use

nodemon index.js

also can set port number using 

set PORT = 5000;

use Joi to do the validation easier

npm i joi --save


